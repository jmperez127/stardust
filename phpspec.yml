suites:
    core_app_suite:
        namespace: Core\App
        spec_prefix: spec

    core_router_suite:
        namespace: Core\Router
        spec_prefix: spec

    core_controller_suite:
        namespace: Core\Controller
        spec_prefix: spec

    fixtures_suite:
        namespace: Fixtures\Controller
        spec_prefix: spec